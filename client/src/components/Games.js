import React from 'react';
import { Grid } from '@material-ui/core';
import ItemCard from './ItemCard';

const Games = () => {
  return <div style={{ height: '100vh', overflow: 'auto' }}>GAMES</div>;
};

export default Games;

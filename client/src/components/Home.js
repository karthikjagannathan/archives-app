import React from 'react';
import { Grid } from '@material-ui/core';
import ItemCard from './ItemCard';

const Home = () => {
  return <div style={{ height: '100vh', overflow: 'auto' }}>HOME</div>;
};

export default Home;
